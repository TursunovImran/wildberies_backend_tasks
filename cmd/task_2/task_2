// Вывод будет таким: 
// [{1 one} {2 two}]

// range занимается итерацией по слайсу slice,  когда перебирается массив или слайс,
//  то в качестве значений возвращается индекс и копия значения элемента.
//  Поэтому мы получили, вывод изначальных структур,  без изменений, т.к. v - это созданная range копия элемента.